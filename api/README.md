# API Backend

This directory will contain the FastAPI backend for FusionCast. It exposes endpoints to retrieve weather predictions generated by the multi-modal AI model and provides APIs for visualizing the fusion layers and sending real-time alerts. Place your routers, services, models, and any auxiliary scripts here. Each component should be well-documented to ease development and maintenance.
